<div class="mycourses-container">
  <div class="row justify-content-center">
    <div class="col-8">
      <div class="text-center">
        <h1 id="home-banner" class="me-5" style="color: #5D3277;"><strong>Mis Cursos</strong></h1>
      </div>
      <br>
      <% if @courses_as_teacher.any? %>
        <div class="me-3 text-center">
          <h2 class="me-3 text-white w-auto" style="background-color: #A16DB7; padding: 10px; border-radius: 5px;">
            Cursos que imparto como docente 👩‍🏫
          </h2>
        </div>
        <div class="mycourses-container mt-5">
          <% @courses_as_teacher.each_slice(3) do |course_group| %>
            <div class="row row-cols-1 row-cols-md-4">
              <% course_group.each do |course| %>
                <div class="ms-4 mb-4 d-flex justify-content-center align-items-center">
                  <%= render partial: "courses/course", locals: { course: course } %>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
      <% end %>
      
      <% if @courses_as_student.any? %>
        <div class="me-3 text-center">
          <h2 class="me-3 text-white w-auto" style="background-color: #A16DB7; padding: 10px; border-radius: 5px;">
            Cursos en los que estoy inscrita 📚
          </h2>
        </div>
        <div class="mycourses-container mt-5">
          <% @courses_as_student.each_slice(3) do |course_group| %>
            <div class="row row-cols-1 row-cols-md-4">
              <% course_group.each do |course| %>
                <div class="ms-4 mb-4 d-flex justify-content-center align-items-center">
                  <%= render partial: "courses/course", locals: { course: course } %>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
      <% end %>

      <% if @courses_as_teacher.empty? && @courses_as_student.empty? %>
        <div class="text-center mt-4 mb-4">
          <h4 class="w-100 text-white" style="background-color: #A16DB7; padding: 10px; border-radius: 5px;">
            ¡Oops! No tienes cursos inscritos aún.
          </h4>
          <br><br>
          <h4 class="mt-3 mb-3">¿Qué quieres hacer? 👀</h4>
          <br>
          <%= link_to '¡Quiero impartir un curso!', new_course_path, class: 'btn btn-flat me-5' %>
          <%= link_to '¡Quiero inscribirme en un curso!', courses_path, class: 'btn btn-flat' %>
        </div>
      <% end %>
      <div class="d-flex justify-content-center text-center mt-5 mb-5 me-5">
        <%= link_to 'Volver a cursos', courses_path, class: 'btn btn-flat me-5 w-25' %>
        <% if @courses_as_teacher.any? %>
          <%= link_to 'Crear nuevo curso', new_course_path, class: 'btn btn-flat w-25', style: 'background-color: #F4A8C1;' %>
        <% end %>

        <% if @courses_as_student.any? && @courses_as_teacher.empty? %>
          <%= link_to '¡Crea tu primer curso!', new_course_path, class: 'btn btn-flat w-25', style: 'background-color: #F4A8C1;' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
